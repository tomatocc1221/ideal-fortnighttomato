# 开发日志 — 2026-05-24

## 完成事项

### Phase 2 核心功能：球队管理系统

- [x] **赛后录入系统** — 管理后台新增「赛后录入」标签页，选择已结束比赛录入比分、进球者、助攻者，自动判定胜负
- [x] **出勤统计** — `/api/stats` 端点计算每位球员正选/候补/请假次数 + 出勤率百分比，后台表格颜色分级展示
- [x] **费用分摊** — 场地费 ÷ 正选人数自动计算人均费用，报名面板实时显示
- [x] **连续缺席提醒** — 连续 ≥2 次请假的球员在后台报名记录中黄色高亮标注
- [x] **比赛 2 小时时长机制** — 比赛开始 2 小时后自动跳过，进入后台等待赛后录入
- [x] **后端服务器** — Node.js `server.js`（端口 3456），REST API 完整 CRUD，JSON 文件数据库
- [x] **报名系统** — 球员姓名+个人密码验证，正选/候补/请假三态，自动补位，请假原因
- [x] **管理后台** — `admin.html`，5 个标签页（队员管理/比赛管理/报名记录/出勤统计/赛后录入）
- [x] **API 封装层** — `js/api.js` 统一管理所有后端请求，失败时静默降级

### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `server.js` | 320 | Node.js 后端服务器 + REST API |
| `admin.html` | 168 | 管理后台页面 |
| `css/admin.css` | 277 | 管理后台样式 |
| `js/admin.js` | 670 | 管理后台交互逻辑 |
| `js/registration.js` | 237 | 报名系统面板 |
| `js/api.js` | 107 | API 客户端封装 |

### 修改文件

| 文件 | 变更 | 说明 |
|------|------|------|
| `index.html` | +103行 | 报名面板 + 管理后台入口 |
| `css/style.css` | +376行 | 报名面板样式 + 报名按钮样式 |
| `js/main.js` | +489行 | API 数据合并 + 报名面板触发 + 倒计时逻辑增强 |

### Supabase 迁移（11:52）

- [x] `js/api.js` 全部 API 方法改为 Supabase REST API，保持接口兼容
- [x] `js/supabase-init.js` 增强 sb 封装 + SHA256 Web Crypto 哈希工具
- [x] `js/admin.js` 管理员 session 24h 过期机制
- [x] `sql/create_tables.sql` 建表 SQL（players, matches, registrations, admins）
- [x] `sql/seed_players.sql` 27 名球员种子数据（默认 PIN: 123456）
- [x] `docs/requirements.md` 更新至 10 个功能区块

### GitHub Actions 自动部署（12:07）

- [x] `.github/workflows/deploy.yml` — push main 触发，部署到 GitHub Pages

### 管理后台保存修复 + 场地恢复手动输入（12:22）

- [x] `js/api.js` CRUD 方法加 try/catch + row 空值检查
- [x] `js/admin.js` save 按钮加 try/catch + alert 错误提示
- [x] `admin.html` 场地改为文本输入框，删除下拉逻辑
- [x] `js/admin.js` 移除 updateVenueSelect 函数

### Supabase 错误消息增强（12:26）

- [x] `js/supabase-init.js` 错误消息读取 Supabase 响应体具体错误详情
- [x] `js/api.js` 同步增强错误传递

### 管理后台交互优化 + 手机端适配（12:49）

- [x] `js/admin.js` 保存后本地数组更新（跳过网络请求）+ 防双击 + 删除异常处理
- [x] `js/supabase-init.js` delete 处理空返回体
- [x] `css/admin.css` 新增 480px 断点 — tab 网格布局、表格紧凑、按钮全宽
- [x] 版本号 v1.0.5 → v1.0.6

### 赛程战果独立页面 + 日期格式统一 + 清理旧文件（下午）

- [x] 删除 `server.js` + `db.json`（已迁至 Supabase）
- [x] 统一全站日期显示为 `YYYY.MM.DD`：`fmtDT()`、报名面板、倒计时栏
- [x] 新建 `fixtures.html`：赛程战果独立页面，赛季筛选 + 分页 + 统计面板
- [x] 新建 `css/fixtures.css`：统计卡片、筛选器、分页器样式 + 移动端适配
- [x] 新建 `js/fixtures.js`：静态数据秒开 + API 后台静默更新、URL 参数支持
- [x] 主页精简：3 场战报 + 3 场赛程 + "查看全部战绩 →"链接
- [x] `fixtures.html` 移动端导航菜单修复
- [x] 性能优化：`content-visibility: auto`、nav GPU 合成层、静态数据先行渲染
- [x] UI 美化：统计卡片渐变 + 悬停动效、返回主页按钮
- [x] 版本号 v1.0.6 → v1.0.7

## 下一步

- [ ] 补充球员头像和比赛照片
- [ ] 管理后台安全加固
- [ ] 首页照片墙 / 精彩瞬间区块
